info:
  name: login (obtener token)
  type: http
  seq: 2

http:
  method: POST
  url: https://api.dentalinfantiloaxaca.xyz/api/login
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "admin@clinicadental.com",
        "password": "password123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
