info:
  name: ContraseñaOlvidada
  type: http
  seq: 4

http:
  method: POST
  url: https://api.dentalinfantiloaxaca.xyz/api/forgot-password
  headers:
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "admin@clinicadental.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
